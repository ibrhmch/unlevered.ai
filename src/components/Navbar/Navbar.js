import React from 'react';
import TopBar from './TopBar';
import NavLinks from './NavLinks';

function Navbar() {
    return (
        <div>
            <TopBar />
            <NavLinks />
        </div>
    );
};

export default Navbar;
